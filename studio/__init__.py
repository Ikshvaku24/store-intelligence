"""Calibration Studio — a separate operator UI (zone drawing + pipeline run).

Not part of the graded API (Process 2). Run with:
    .venv/Scripts/python.exe -m uvicorn studio.app:app --port 8090
"""
